# getpixelcode
This project is a simple and creative Python-based tool that converts any image into ASCII pixel art. It reads an input image, processes each pixel, and transforms the entire picture into a text-based artistic version.
